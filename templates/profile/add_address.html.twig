{% extends 'base.html.twig' %}

{% block title %}Add Address - The Editorial Roast{% endblock %}

{% block content %}
<section class="min-h-[85vh] bg-surface-container-low flex items-center justify-center py-20 px-8">
    <div class="max-w-2xl w-full bg-surface p-12 rounded-2xl shadow-sm relative">
        <div class="mb-8 flex items-center gap-4">
            <a href="javascript:history.back()" class="text-on-surface-variant hover:text-primary transition-colors flex items-center gap-1 font-bold text-sm">
                <span class="material-symbols-outlined text-lg">arrow_back</span>
                Back
            </a>
        </div>

        <h1 class="font-headline text-3xl font-extrabold text-on-surface mb-6">Add New Address</h1>
        
        {{ form_start(form, {'attr': {'class': 'space-y-6', 'novalidate': 'novalidate'}}) }}
            
            <div>
                {{ form_label(form.label, null, {'label_attr': {'class': 'block font-label text-[10px] uppercase tracking-[0.2em] text-outline mb-2'}}) }}
                {{ form_widget(form.label, {'attr': {'class': 'w-full bg-surface-container-low border border-outline/30 focus:border-primary focus:ring-1 focus:ring-primary rounded-md px-4 py-3 text-on-surface font-body transition-colors'}}) }}
                {{ form_errors(form.label) }}
            </div>

            <div>
                {{ form_label(form.line1, null, {'label_attr': {'class': 'block font-label text-[10px] uppercase tracking-[0.2em] text-outline mb-2'}}) }}
                {{ form_widget(form.line1, {'attr': {'class': 'w-full bg-surface-container-low border border-outline/30 focus:border-primary focus:ring-1 focus:ring-primary rounded-md px-4 py-3 text-on-surface font-body transition-colors'}}) }}
                {{ form_errors(form.line1) }}
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    {{ form_label(form.city, null, {'label_attr': {'class': 'block font-label text-[10px] uppercase tracking-[0.2em] text-outline mb-2'}}) }}
                    {{ form_widget(form.city, {'attr': {'class': 'w-full bg-surface-container-low border border-outline/30 focus:border-primary focus:ring-1 focus:ring-primary rounded-md px-4 py-3 text-on-surface font-body transition-colors'}}) }}
                    {{ form_errors(form.city) }}
                </div>
                <div>
                    {{ form_label(form.postalCode, null, {'label_attr': {'class': 'block font-label text-[10px] uppercase tracking-[0.2em] text-outline mb-2'}}) }}
                    {{ form_widget(form.postalCode, {'attr': {'class': 'w-full bg-surface-container-low border border-outline/30 focus:border-primary focus:ring-1 focus:ring-primary rounded-md px-4 py-3 text-on-surface font-body transition-colors'}}) }}
                    {{ form_errors(form.postalCode) }}
                </div>
            </div>

            <div>
                {{ form_label(form.country, null, {'label_attr': {'class': 'block font-label text-[10px] uppercase tracking-[0.2em] text-outline mb-2'}}) }}
                {{ form_widget(form.country, {'attr': {'class': 'w-full bg-surface-container-low border border-outline/30 focus:border-primary focus:ring-1 focus:ring-primary rounded-md px-4 py-3 text-on-surface font-body transition-colors'}}) }}
                {{ form_errors(form.country) }}
            </div>

            <button class="w-full bg-primary-gradient text-on-primary font-bold py-4 rounded-md mt-4 transition-transform active:scale-[0.98] shadow-sm">
                Save Address
            </button>
            
        {{ form_end(form) }}
    </div>
</section>
{% endblock %}
